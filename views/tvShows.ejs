<% include ./partials/header %>

    <div class="container">
      <div class="row">
        <!-- I'm going to re-design this with carousels later... (one carousel for each category?) -->
      <% for(var i=0; i<tvShows.results.length; i++) { %>
        <div class="col">
          <div class="card" style="width: 250px;">
            <img class="card-img-top" src="http://image.tmdb.org/t/p/w342/<%= tvShows.results[i].poster_path %>" alt="Card image cap">
            <div class="card-body">
              <span class="card-title"><%= tvShows.results[i].name %></span><br>
              <a href="/watch/<%= tvShows.results[i].id %>" class="btn btn-primary">Watch</a>
            </div>
          </div>
        </div>
      <% } %>
      </div>
    </div>
    <% include ./partials/footer %>
