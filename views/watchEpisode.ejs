<% include ./partials/header %>
      <!-- Sandbox only allows same origin and scripts -->
    <div class="container extra-padding">
      <div class="row">
        <div class="col-lg-8">
          <h1><%= tvInfo.name %></h1>
          <h2 class="subtext">S<%= episodeInfo.season_number %>:E<%= episodeInfo.episode_number %> "<%= episodeInfo.name %>"</h2>
          <p><%= episodeInfo.overview %></p>
          <div class="dionysus-video">
            <video id="my-video" class="video-js" autoplay controls preload="auto"  data-setup="{}">
              <source src="http://ctvod.run/ctv/guestmod/<%= tvInfo.name.replace(/[^\w\s]/gi, '').replace(/\s+/g, '.').toLowerCase() %>.s<%= seasonNum%>e<%= episodeNum %>.mp4">
              <source src="http://ctvod.run/ctv/guestmod/<%= tvInfo.name.replace(/[^\w\s]/gi, '').replace(/\s+/g, '.').toLowerCase() %>.s<%= seasonNum%>e<%= episodeNum %>.mkv">

              Please update your browser to the latest version of Chrome to stream this content
            </video>
          </div>
        </div>
      </div>
    </div>
<style>
body {
  background-image: linear-gradient(to right, rgba(0,0,0,0.9),rgba(0,0,0,0.6)), url(http://image.tmdb.org/t/p/original<%= tvInfo.backdrop_path %>);
  background-position: center;
  background-size: cover;
}
</style>
<% include ./partials/footer %>
